// very simple mvc framework.

// includes ways to communicate via rest and soap.

// work in progress.

// the router file does not really handle all requests accordingly. just a simple simulation.

// implement better json routing. the current implementation is a very "work around" type of approch.

// rest and soap request only very simple. 

// rest should really implement the GET, POST, PUT, DELETE approach as described by r.fielding.